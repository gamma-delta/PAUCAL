repositories {
    mavenCentral()
}

dependencies {
    // Different jankson than the shaded one, this is just for json5 -> json
    implementation("blue.endless:jankson:1.2.2")
    implementation group: 'com.diluv.schoomp', name: 'Schoomp', version: '1.2.6'
}
